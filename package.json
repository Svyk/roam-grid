{
  "name": "roam-grid",
  "version": "0.14.0",
  "private": true,
  "description": "Native-first advanced tables for Roam Research",
  "type": "module",
  "main": "extension.js",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.js",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "verify:generated": "node scripts/verify-generated.mjs",
    "check": "node --check src/extension.js && npm test && npm run scan:secrets && npm run verify:generated",
    "dev": "npm run build -- --watch"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
